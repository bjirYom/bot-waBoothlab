<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Login</title>
  <style>
    body {
      font-family: "Segoe UI", Arial, sans-serif;
      background-color: #f8f9fa;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
      padding: 10px; /* agar ada space di mobile */
      box-sizing: border-box;
    }
    .login-container {
      background: #fff;
      padding: 25px 20px;
      border-radius: 10px;
      box-shadow: 0 6px 15px rgba(0,0,0,0.08);
      width: 100%;
      max-width: 360px; /* max lebar agar tidak terlalu besar */
      box-sizing: border-box;
    }
    .login-container h1 {
      text-align: center;
      margin-bottom: 25px;
      color: #333;
      font-size: 22px;
    }
    .login-container input {
      width: 100%;
      padding: 10px;
      margin: 10px 0; /* hanya atas dan bawah */
      border: 1px solid #ccc;
      border-radius: 6px;
      font-size: 14px;
      transition: border-color 0.3s ease, box-shadow 0.3s ease;
      box-sizing: border-box;
    }
    .login-container input:focus {
      border-color: #007bff;
      outline: none;
      box-shadow: 0 0 5px rgba(0,123,255,0.3);
    }
    .login-container button {
      width: 100%;
      padding: 10px;
      background: #007bff;
      border: none;
      color: white;
      font-size: 15px;
      border-radius: 6px;
      cursor: pointer;
      transition: background 0.3s ease;
      margin-top: 5px;
      box-sizing: border-box;
    }
    .login-container button:hover {
      background: #0056b3;
    }

    /* Responsive tweaks jika mau */
    @media (max-width: 400px) {
      .login-container {
        padding: 20px 15px;
        max-width: 100%;
      }
      .login-container h1 {
        font-size: 20px;
      }
    }
  </style>
</head>
<body>
  <div class="login-container">
    <h1>Login</h1>
    <form action="/login" method="POST">
      <input type="text" name="username" placeholder="Username" required />
      <input type="password" name="password" placeholder="Password" required />
      <button type="submit">Masuk</button>
    </form>
  </div>
</body>
</html>
