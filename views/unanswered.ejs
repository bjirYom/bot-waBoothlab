<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <title>Pesan Tak Terjawab</title>
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: 'Segoe UI', sans-serif;
      display: flex;
      min-height: 100vh;
      background-color: #f0f2f5;
    }

    .sidebar {
      width: 220px;
      background-color: #343a40;
      color: #fff;
      display: flex;
      flex-direction: column;
      padding: 20px;
    }

    .sidebar h2 {
      margin-bottom: 30px;
      font-size: 22px;
      color: #ffc107;
      text-align: center;
    }

    .sidebar a {
      color: #ccc;
      text-decoration: none;
      margin: 10px 0;
      padding: 10px;
      border-radius: 4px;
      display: block;
      transition: 0.3s;
    }

    .sidebar a:hover,
    .sidebar a.active {
      background-color: #495057;
      color: #fff;
    }

    .main {
      flex: 1;
      padding: 30px;
    }

    h1 {
      margin-bottom: 20px;
      color: #333;
    }

    .message-list {
      background-color: #fff;
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.05);
    }

    .message-item {
      border-bottom: 1px solid #eee;
      padding: 15px 0;
    }

    .message-item:last-child {
      border-bottom: none;
    }

    .nomer {
      font-weight: bold;
      color: #17a2b8;
      margin-bottom: 5px;
    }

    .content {
      margin: 5px 0;
      color: #555;
      white-space: pre-line;
    }

    .timestamp {
      font-size: 12px;
      color: #888;
    }

    @media (max-width: 768px) {
      body {
        flex-direction: column;
      }

      .sidebar {
        width: 100%;
        flex-direction: row;
        justify-content: space-around;
        padding: 10px;
      }

      .sidebar h2 {
        display: none;
      }

      .main {
        padding: 20px;
      }
    }
  </style>
</head>
<body>
  <div class="sidebar">
    <h2>📚 Boothlab</h2>
    <a href="/crud">📋 Context List</a>
    <a href="/crud/system" >🧠 System Prompt</a>
    <a href="/unanswered" class="active">❌ Pesan error</a>
    <a href="/connect" target="_blank">💬 Hubungkan ke WhatsApp</a>
  </div>

  <div class="main">
    <h1>❌ Pesan yang Tidak Terjawab</h1>

    <div class="message-list">
      <% if (messages.length === 0) { %>
        <p>Tidak ada pesan yang belum terjawab.</p>
      <% } else { %>
        <% messages.forEach(msg => { %>
          <div class="message-item">
            <div class="nomer">
  📱 
  <% if (msg.nomer) { %>
    <a href="https://wa.me/<%= msg.nomer.replace(/\D/g, '') %>" target="_blank" style="color: #17a2b8; text-decoration: underline;">
      <%= msg.nomer %>
    </a>
  <% } else { %>
    Tidak diketahui
  <% } %>
</div>

            <div class="content"><%= msg.message %></div>
            <div class="timestamp">🕒 <%= new Date(msg.created_at).toLocaleString('id-ID') %></div>
          </div>
        <% }) %>
      <% } %>
    </div>
  </div>
</body>
</html>
